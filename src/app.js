import React from "react";
import ReactDOM from "react-dom";
import { AppWrapper } from "./global.styled";
const template = <AppWrapper>hello react</AppWrapper>;

ReactDOM.render(template, document.getElementById("root"));
